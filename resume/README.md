# Résumé source

Edit `resume.md`, then rebuild the downloadable DOCX:

```bash
pip install python-docx
python3 build_docx.py
```

This writes an ATS-safe DOCX to `../assets/Jonathan-Brenes-Fernandez-Resume.docx`
(single column, standard headings, plain bullets, no tables/images).
